#!/usr/bin/env bash

set -Eeuo pipefail

panic() {
  echo "FATAL: ${*}" >&2
  exit 1
}

test -x "${ASDF_DOWNLOAD_PATH}/pre-commit" || panic "pre-commit has not been downloaded yet."
mkdir -p "${ASDF_INSTALL_PATH}/bin"
mv "${ASDF_DOWNLOAD_PATH}/pre-commit" "${ASDF_INSTALL_PATH}/bin/pre-commit"
